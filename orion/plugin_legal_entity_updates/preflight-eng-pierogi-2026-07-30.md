# Pre-flight — provider billing informations vs legal entities

|  |  |
|---|---|
| namespace | eng-pierogi |
| generated | 2026-07-30T11:10:03.711Z |
| providers compared | 1 |
| verdict | **PASS** — 0 blocked, 0 differ, 0 consistent |

## Blocked — required fields unusable in the legal entity

None — every provider carries the fields its country requires.

## provider=153 (individual, ?)

0/3 comparable fields agree *(not an e-invoicing country — nothing required)*.

| Field | Required? | Provider billing (shedul) | Legal entity (`fields` jsonb) | Present? |
|---|---|---|---|---|
| first name |   | — | `individual.name.firstName` = bleron | ⚠ legal entity only |
| last name |   | — | `individual.name.lastName` = qorri | ⚠ legal entity only |
| country |   | — | `_column.country_code` = ES | ⚠ legal entity only |

## KYC / payments gate

| Provider | Payments | KYC provider (adyen LE) | Tier | Gate | Basis |
|---|---|---|---|---|---|
| 153 | not_set | ∅ not synced | — | ✓ allowed | payments not_set — KYC not required |

Mirrors `AccountingDocuments.EInvoicing.Common.PaymentsKycGate`: payments not enabled → allowed (KYC irrelevant); payments enabled + approved → allowed; payments enabled + not approved **or not synced** → `{:error, :kyc_not_approved}`.

| Signal | Source | Exact? |
|---|---|---|
| payments enabled | `shedul.providers.fresha_pay` (enum `not_set`/`enabled`/`disabled`) | yes |
| synced to KYC provider | `legal_entities.adyen_platform_legal_entity_id` | yes |
| verification status | `adyen_platform.legal_entity_verifications.state` | **no** — authoritative value is behind the adyen-platform RPC |

Rows in *italics* are inferred, not definitive. **`? unknown` means payments are enabled and the entity is synced, but no verification record exists — this report will not claim PASSED from the database.**

## Where "required" comes from

Both validators fetch the legal entity via `GetLegalEntityInvoiceDetails` and hard-fail with `{:error, :missing_required_fields}`:

| Field | ES / IT | KSA (SA) |
|---|---|---|
| `company_name` | required | not checked — taken from the request |
| `address` (street), `city`, `postal_code`, `state_province` | required | required |
| `tax_number` | required — `TAX_IDENTIFICATION_NUMBER` | required — `TAX_NUMBER` (ZATCA TRN) |
| `company_registration_number` | — | **required** |
| `building_number`, `district` | — | **required** |
| `country_code` | not checked | not checked |

- `SA` → `AccountingDocuments.EInvoicing.Comarch.LegalEntityBillingDetails` `@required_fields`
- `ES` / `IT` → `AccountingDocuments.EInvoicing.Common.LegalEntityBillingDetails` `@required_fields`

The country comes from `account_configurations.country_code` — that's what `BillingDetailsPolicy` dispatches on. Countries with no entry have no required set.

The KSA `tax_number` is a *different identifier kind*, so present-and-equal here is necessary but not sufficient.

### KSA format rules

Presence is not the only bar — `AccountingDocuments.Helpers.ValidationHelpers` also enforces shape, so a present-but-malformed value still fails:

| Field | Rule | Source |
|---|---|---|
| `company_registration_number` | exactly 10 characters | `valid_ksa_crn?` |
| `tax_number` | 15 digits, starts `3`, ends `03` — `~r/^3\d{12}03$/` | `valid_ksa_tax_id?` |

<sub>Generated by `plugin_legal_entity_updates.js --preflight` — read-only.</sub>
